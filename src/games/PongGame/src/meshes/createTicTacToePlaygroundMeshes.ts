import {
    MeshBuilder,
    Color3,
    StandardMaterial,
} from '@babylonjs/core';
import { GameContext } from '../GameContext.ts';

export function createTicTacToePlayroundMeshes(): void {
    const scene = GameContext.scene!;
    const { width, depth } = GameContext.size;


}
